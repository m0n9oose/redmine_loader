Congrats, your tasks

  <% @issues.each do |issue| -%>
    * Tracker: <%= h("#{issue[:tracker_name]}") %> / Issue title: <%=h issue[:title] %>
  <% end -%>

were successfully imported to project <%= @project.name %>.

You can find it here <%= @issues_url %>.
